#using SokobanImportExport.*
#using AttrCondDefLibrary.*

#abstract #rule NextLinkRule #with SokobanImportExport

#source { 
	++ e:Entry

	++ ne:Entry
	
	++ link:NextLink {
		++ -src->e
		++ -trg->ne
	}
	
	extBoard:ExtendedBoard {
		++ -links->link
	}
}